{
  "name": "tab-isolator",
  "version": "1.0",
  "scripts": {
    "postinstall": "husky install",
    "format": "prettier --loglevel warn --write \"**/*.{js,json,md}\"",
    "lint-staged": "lint-staged",
    "lint": "eslint src/**/*.js"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "husky": "^8.0.1",
    "lint-staged": "^12.4.1",
    "prettier": "^2.6.2"
  }
}
